# Copy to /etc/whip-stream.env on the Pi (root:root, mode 600) and fill in real values.
# Read by both whip-camera-stream.service and whip-watchdog.service via EnvironmentFile=.
WHIP_URL=https://vanicall.fly.dev/whip/<ingest-id>
WHIP_TOKEN=<ingest-bearer-token>
